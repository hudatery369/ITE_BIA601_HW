# 🚀 طريقة التشغيل الكاملة باستخدام venv

## الخطوة 1: إنشاء بيئة افتراضية (venv)

### على Windows:
```bash
# إنشاء البيئة الافتراضية
python -m venv venv

# تفعيل البيئة الافتراضية
venv\Scripts\activate
```

### على Linux/Mac:
```bash
# إنشاء البيئة الافتراضية
python3 -m venv venv

# تفعيل البيئة الافتراضية
source venv/bin/activate
```

## الخطوة 2: تثبيت المكتبات

بعد تفعيل البيئة الافتراضية:

```bash
pip install -r requirements.txt
```

## الخطوة 3: تشغيل التطبيق

```bash
python app.py
```

سيبدأ الخادم على: `http://localhost:5000`

افتح المتصفح على العنوان: `http://127.0.0.1:5000`

## الخطوة 4: استخدام التطبيق

1. **اقرأ معلومات المشروع** في الصفحة الرئيسية
2. **ارفع ملف CSV** إما بالضغط على الزر أو بسحب وإفلات الملف
3. **اختر عمود الهدف** من القائمة المنسدلة
4. **اضبط الإعدادات** (اختياري):
   - حجم المجتمع (Population Size)
   - عدد الأجيال (Generations)
   - معدل الطفرات (Mutation Rate)
5. **اضغط "ابدأ التحليل"**
6. **استعرض النتائج** التي ستظهر تلقائياً

## إنشاء بيانات تجريبية (اختياري)

إذا كنت تريد تجربة التطبيق ببيانات تجريبية:

```bash
python utils/example_data.py
```

سيتم إنشاء ملف `example_data.csv` يمكنك استخدامه.

## إيقاف التشغيل

اضغط `Ctrl + C` في نافذة Terminal/Command Prompt

## إلغاء تفعيل البيئة الافتراضية

```bash
deactivate
```

## ملاحظات

- تأكد من أن Python مثبت على جهازك (3.8 أو أحدث)
- التطبيق يدعم ملفات CSV فقط
- يجب أن يحتوي الملف على أعمدة رقمية
- الخوارزمية الجينية قد تأخذ بعض الوقت حسب حجم البيانات
- الملفات المرفوعة تُحذف تلقائياً بعد المعالجة

## استكشاف الأخطاء

### مشكلة: "python: command not found"
- استخدم `python3` بدلاً من `python` على Linux/Mac

### مشكلة: "ModuleNotFoundError"
- تأكد من تفعيل البيئة الافتراضية
- قم بتثبيت المكتبات مرة أخرى: `pip install -r requirements.txt`

### مشكلة: البورت 5000 مستخدم
- غير البورت في `app.py` من `port=5000` إلى أي بورت آخر مثل `port=5001`

